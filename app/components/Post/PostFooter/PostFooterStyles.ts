import {StyleSheet} from 'react-native';

export const PostFooterStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    height: 40,
    alignItems: 'center'
  },
  likeIcon: {
    marginLeft: 10,
    marginRight: 20
  },
  postDescription: {}
});
